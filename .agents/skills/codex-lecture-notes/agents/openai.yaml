interface:
  display_name: "Codex 讲义"
  short_description: "按确认大纲逐单元写入并维护中文 Quarto 讲义"
  default_prompt: "Use $codex-lecture-notes to confirm the full heading outline, then confirm each unit outline and write it directly to the target QMD for file-based review."
